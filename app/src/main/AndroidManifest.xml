<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ministockbitapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_stockbit_black_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_stockbit_black_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiniStockbitApp">
        <activity
            android:name=".ui.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SecondApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.StockbitActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SecondApp"/>
    </application>

</manifest>